#include "main.h"
/**
  *_abs -  to return the absolute interger
  *@n: the number to be returned
  *
  *Return: absolute value of the interger
  *
  */
int _abs(int n)
{

	if (n >= 0)
		return (n);
	else
		return (0);
}
